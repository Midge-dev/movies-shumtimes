services:
  - type: web
    name: reelay-sync
    runtime: node
    plan: free
    rootDir: relay
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: RELAY_TOKEN
        generateValue: true
