services:
  - type: web
    name: shumtimes-relay
    runtime: node
    plan: free
    rootDir: relay
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: RELAY_TOKEN
        generateValue: true
