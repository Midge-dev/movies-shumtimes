*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
/shared/build
/relay/node_modules
# Real release signing identity — unlike app/debug.keystore (checked in
# deliberately, see build.gradle.kts), this one is the actual key an app
# update would need to match forever, so it must never leave this machine.
/app/release.keystore
